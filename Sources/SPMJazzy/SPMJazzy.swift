/// SPMJazzy does it all.
///
/// OK, not really. It's just a test for Jazzy's documentation generation
public struct SPMJazzy {
    /// The text to display
    public private(set) var text = "Hello, World!"

    /// Initializes an SPMJazzy
    public init() {
    }
}
